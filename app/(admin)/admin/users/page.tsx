import { PlaceholderPanel } from "@/components/ui/placeholder-panel";

export default function AdminUsersPage() {
  return (
    <PlaceholderPanel
      title="User administration"
      description="Manage roles, verification states, and support interventions from a dedicated admin module."
      hint="Future user-management actions should route through auth and administration services."
    />
  );
}
