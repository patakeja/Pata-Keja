import { PlaceholderPanel } from "@/components/ui/placeholder-panel";

export default function AdminBookingsPage() {
  return (
    <PlaceholderPanel
      title="Booking operations"
      description="Queue disputes, manual intervention, and reservation audit tooling can be built here."
      hint="The booking foundation already exposes the core states needed for future operations workflows."
    />
  );
}
